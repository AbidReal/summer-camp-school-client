import Slider from "./Slider";

const Home = () => {
  return (
    <div>
      Home with 4 sections. slider, top classes, top instructors, bonus
      <Slider></Slider>
    </div>
  );
};

export default Home;
